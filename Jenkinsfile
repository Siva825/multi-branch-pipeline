 pipeline{
    agent any
    environment{
        name='siva'
        course = 'Devops'
    }
    stages{
        stage('Build'){
            steps{
                echo "This is ${name}"
                echo "This is ${course}"
            }
        }
    }
}
